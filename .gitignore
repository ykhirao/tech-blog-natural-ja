# secrets
.env
.env.*
!.env.example

# data (API から取得した生データ・中間生成物)
# processed/ も再生成できるうえ巨大になるので追跡しない。
# 指標そのものではなく、集計結果(docs/ 配下)を成果物として残す。
data/raw/
data/interim/
data/processed/
data/*.log
# 往復翻訳の入出力。記事の本文(最大1,500字)がそのまま入るので追跡しない
data/roundtrip/
*.jsonl.gz

# python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# node
node_modules/

# os
.DS_Store

# node (textlint の試用でできるもの)
package.json
package-lock.json

# 誤って混入した作業ファイル
prevState
{processing}
typescript
translation.txt
