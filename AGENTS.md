# tech-blog-natural-ja

Qiita の記事を大規模に計測して、**技術記事に特化した日本語の文体プロファイル**を作る。
最終的な成果物は、実測に基づく閾値・textlint 設定・Agent Skill。

## このプロジェクトの立ち位置

**同種の汎用スキルはすでに存在する。** `coji/natural-japanese`(★1057, MIT, Python)が
実質の決定版で、burstiness・MTLD・TTR・太字・箇条書きの検出をすでに実装している。
`stop-ai-slop-jp`(★453)など類似 OSS は15件以上あり、この分野は飽和している。

**空いているのは1点だけ**: `natural-japanese` の `lint.py` に "qiita" の文字列は0件。
閾値は汎用ビジネス文書で校正されていて、**技術記事で校正されていない**。
`calibrate.py` という校正ハーネスまで用意されているのに、誰もやっていない。

だからこのプロジェクトは**再実装ではなく、Qiita 実測による校正プロファイル**を作る。

### 実測で見えている手がかり

2024-12-01 の706件を計測したところ、`burstiness_mora` の中央値が **-0.258**。
既存 lint の閾値は **-0.24**(これ未満で警告)なので、**Qiita 記事の半分以上が
既存閾値では警告になる**。技術記事では閾値そのものが合っていない可能性が高い。
ただし AI の影響と技術記事固有の性質が混ざっているので、
AI 以前(2019-2022)の値と比べて切り分ける必要がある。

## 絶対にやらないこと

**記事単位で「これは AI が書いた」と判定する機能は作らない。**

- 非ネイティブ英語話者の誤検出率 **61%**(Liang et al., Patterns 2023)
- Vanderbilt 大は Turnitin の AI 検出を停止(FPR 1% でも年750名を誤認)
- OpenAI は自社検出器を撤回
- Sadasivan et al. は理論的な検出不可能性を証明

Qiita には非ネイティブ日本語話者も書いている。誤検出は実在の書き手を不当に貶める。
出していいのは**指標値と、参照分布からの距離**まで。ラベル付けはしない。
これは元ネタの `natural-japanese` の思想(検出は疑いの提示に留め、判断は人間)とも一致する。

## ディレクトリ

```
scripts/            資産。すべて uv 単体実行(PEP 723)で依存を自己完結
  fetch_qiita.py       Qiita API から取得。日単位・レジューム対応
  status_qiita.py      取得の進捗と穴を一覧
  metrics.py           指標計算(ライブラリ兼CLI)。指標定義の唯一の置き場
  build_dataset.py     除外基準をかけて分析用データセットを作る
  inspect_outliers.py  分布と外れ値の診断。スクリプト改善のための道具
  timeline.py          月をまたいだ推移。形態素解析を使わないので軽い
  roundtrip_queue.py   往復翻訳のジョブキュー。並行実行しても壊れない
  weekly.py            週ごとの推移。月次で見つけたら必ずこれで確かめる
  split_by.py          ある特徴で記事を分けて全指標を比べる(指標どうしの関係)
  within_author.py     本人が変えたのか書き手が入れ替わったのかを分ける
  check_repo.py        公開前の点検。リンク・コマンド・混入を機械で見る
  check_article.py     記事の数字を実データと照合する
  confounds.py         対抗仮説をまとめて潰す(話題・書き手・人気・分野)
  by_domain.py         タグで分野に分けて測る。分野の定義もここ
  title_trend.py       タイトルの推移。数字の用法も分類する
  heading_trend.py     見出しの数・長さ・階層
  code_trend.py        コードブロックの量と言語指定
  list_trend.py        箇条書きの入れ子とリンク
  structure_trend.py   引用・段落・画像
  edit_rate.py         公開後に直しているか(updated_at を使う)
  verify_ai_words.py   p1ass 辞書を実測で検証する
  rule_hits.py         textlint ルールの年ごとの検出率
data/
  raw/         API の生データ。qiita_YYYY-MM-DD.jsonl + .manifest.json
  interim/     試行錯誤の中間物
  processed/   metrics_YYYY-MM.jsonl / excluded_YYYY-MM.jsonl
  roundtrip/   往復翻訳の入出力。記事本文が入る
docs/          調査メモ
src/           公開成果物(textlint ルール・プロンプト・集計値)
article/       Qiita 投稿用の記事
```

**`data/` は丸ごと `.gitignore` 済み。** 中身は他人の記事本文なので、
追跡すると公開リポジトリに著作物が混入する。`.env` も除外済み。

公開していいのは `src/data/` の集計値だけ(428KB、本文を含まない)。

## 公開前の点検

commit の前ではなく、**公開の前に** `./scripts/check_repo.py` を通す。

```
入口のファイル / Markdown のリンク / 追跡外への参照 /
ドキュメントのコマンド / 大きい追跡ファイル
```

過去にここで実際に見つかった問題:

- `data/roundtrip/` が `.gitignore` から漏れ、記事本文30MBが追跡されていた
- README に `--from` と書いたが、実際の引数は `--months` だった
- `prevState` `typescript` など、`git add -A` で拾った無関係なファイル
- タイトルの「74万記事」が、測り直して81万本になった後も残っていた

## 記事の数字は目視で追わない

記事は2,000行を超え、データを測り直すたびに数字がずれる。
`scripts/check_article.py` が実データと照合する(`check_repo.py` から呼ばれる)。

拾えるのは決まった書き方だけなので、**これを通しても保証にはならない**。
ただし目視より確実で、実際に見落としを2件見つけた。

照合するときの注意:

- **「体言止め差」のような差分の列は照合しない。** 値そのものではない
- **「体言止め(1-25)」のような期間限定の列も照合しない。** 12月1〜25日だけの値
- **概数は5%まで許す。** 10%にしたら「81万→74万」(8.8%ずれ)が通り抜けた
- **「122万件」は取得件数、「81万本」は分析対象。** 別の数なので両方を許す

**未追跡のファイルも点検の対象にしている。** commit すれば公開されるものは
同じ扱いにしないと、書いたばかりの README の誤りが素通りする。

## 取得の設計

**10,000件の壁**: Qiita API は `page<=100` × `per_page<=100` で
1クエリ1万件が上限。12月は月13,000〜19,500件あるので月単位では取りこぼす。
→ **日単位**で取る。日あたりのピークは1,000件程度なので安全。

**完了の記録**: 日ごとに `.manifest.json` を書き、`complete` を明示する。
ファイルの存在だけでは「中断途中」と「完走」を区別できないため。
`status_qiita.py` がこれを読んで、未完了の月と再開コマンドを出す。

```bash
./scripts/fetch_qiita.py --range 2019-01:2026-09   # 全期間
./scripts/fetch_qiita.py --years 2020,2025         # 指定年の1〜12月
./scripts/fetch_qiita.py --months 2024-12          # 指定月
./scripts/status_qiita.py --resume-cmd             # 進捗と再開コマンド
```

レートリミットは認証ありで 1000 req/h。全期間で約2,500リクエストなので
数時間で終わる。中断しても再実行すれば続きから走る。

## 12月(アドベントカレンダー)の扱い

12月は毎年6月の **1.5〜2.2倍**の記事数(実測。2019-2025の7年間すべて)。
普段書かない人が書くので、**書き手の層が違う**という交絡がある。

| 年 | 6月 | 12月 | 比 |
|----|-----|------|-----|
| 2019 | 9,671 | 18,465 | 1.91x |
| 2022 | 7,405 | 15,405 | 2.08x |
| 2025 | 9,312 | 19,536 | 2.10x |

文体の差が出ても「AI のせい」か「書き手が違うから」かを分離する必要がある。
`user_items_count` と他月の投稿有無から**常連/非常連**を判定して層別に見る。
6月は常連中心の対照群として使える。

## 除外基準

`build_dataset.py` が適用。2024-12 の実測で除外率16%。

- `short_body(<300)` 地の文300字未満(逆瀬川氏の基準に合わせた)
- `not_japanese` 日本語文字が地の文の30%未満
- `slide_mode` スライドモードは文章構造が別物
- `few_sentences(<5)` 文数が少ないと burstiness が意味を成さない
- `private` / `empty_body`

**除外した記事も理由付きで `excluded_*.jsonl` に残す。**
除外が厳しすぎないかを後から検証できるようにするため。

## 作業の分担(worktree で並行する場合)

スクリプトは疎結合にしてあるので、以下は別 worktree で同時に進められる。

| 単位 | 触るファイル | 前提 |
|------|------------|------|
| 取得 | `scripts/fetch_qiita.py` `status_qiita.py` | なし |
| 指標 | `scripts/metrics.py` | `data/raw` があれば可 |
| 集計・可視化 | 新規 `scripts/aggregate.py` | `data/processed` |
| textlint 連携 | 新規 `textlint/` | 閾値が決まってから |
| Skill 化 | 新規 `skill/` | 閾値が決まってから |

`data/` は共有したいので、worktree を切るときは
`data/` を実体のあるパスへ symlink するか、`--range` で月を分けて取る。
同じ日を2プロセスが同時に書いても、`.tmp` → `rename` なので壊れはしない。

## 月次で見つけたら、週次で確かめる

月の区切りは分析上の都合であって、変化がそこで起きる理由はない。

実際にやらかした例。引用と箇条書きの入れ子が「2026年3月に跳ねた」ように
見えたので、複数の指標が同じ月を指していると書きかけた。
週ごとに測り直すと**2月上旬から連続して動いていた**だけだった
([docs/structure-trend.md](docs/structure-trend.md))。

```bash
./scripts/weekly.py --from 2026-01 --to 2026-06
```

`--min-articles` の既定は300。年末年始や取得の切れ目で薄い週ができるので、
そのまま並べると跳ねて見える。

## 方針

- **判断より計測**。通説ではなく実測で閾値を決める。
  実際、元記事でも「しましょう」は増えるどころか減っていたし、
  coji 氏の計測でも体言止め・文頭反復・「最後に」は通説と逆だった。
- **外れ値を見る**。指標が変な値を取ったら `inspect_outliers.py` で
  実記事を目視し、前処理のバグか本物かを切り分けてからスクリプトを直す。
- **除外を可逆にする**。落とした記事は理由付きで残す。
- 指標の定義は `metrics.py` 1か所。textlint もプロンプトもここを参照する。
